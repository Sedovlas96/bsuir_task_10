	«адание 10. ќперации с матрицами

	ѕри решении данной задачи использовалс€ €зык программировани€ Python версии 3.6.1

	¬ архиве представлены следующие файлы:
1. input_file.txt		Ц файл с матрцией;
2. input_file2.txt		Ц файл с матрцией;
3. input_file3.txt		Ц файл с матрцией;
4. functions.py		Ц python-файл, в котором наход€тс€ следующие функции:
	get_matrix_from_file	Ц считывает матрицу из файла и возвращает numpy-массив;
	matrix_write	Ц записывает матрицу в файл в указанном формате;
	matrix_sum	Ц возвращает сумму двух матриц;
	matrix_dot	Ц возвращает произведение матриц;
	matrix_determinant	Ц возвращает определитель матрицы;
	matrix_sin		Ц возвращет Sin(X), где X Ц матрица.
5. task_10_matrix.py		Ц python-файл, в котором загружаютс€ матрицы из файлов, а также вывод€тс€ результаты всех условий, поставленных в задаче (запись в файл, сумма и произведение матриц, определитель и синус матрицы);	
6. output_matrix.txt		Ц текстовый файл, в который записалась матрица;

	„тобы посмотреть на результат выполнени€ программы, необходимо запустить файл task_10_matrix.py.  ƒл€ решени€ задачи € использовал среду разработки PyCharm 2017.1.4.
ѕри этом, необходимо установить библиотеку numpy (если она не установлена). 
	¬ PyCharm это делаетс€ так: File Ц> Settings Ц> Project Interpreter Ц> Install. ƒалее в строке поиска нужно ввести numpy и нажать Install Package
	≈сли нужно утсановить библиотеку из командной строки, то деалетс€это так:
	¬ Linux:		sudo pip3 install numpy
	¬ Windows:	pip3 install numpy
